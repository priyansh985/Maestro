# Pinned runtime deps for the offline reproduction (no LLM key required).
fastapi==0.115.6
uvicorn[standard]==0.32.1
websockets==13.1
pydantic==2.10.6
scapy==2.6.1
psutil==6.1.0
omegaconf==2.3.0
pyyaml==6.0.2
rich==13.9.4
pandas==2.2.3
tabulate==0.9.0
numpy==2.1.3
httpx==0.28.1
jinja2==3.1.4
python-multipart==0.0.20
# dev / test
pytest==8.3.4
pytest-asyncio==0.24.0
ruff==0.6.9
mypy==1.13.0
types-pyyaml==6.0.12.20240917
types-psutil==6.1.0.20241221
# Optional extras (install explicitly if you want the real providers):
#   pip install -e ".[openai]"   # OpenAI-backed reasoning (Assumption A1)
#   pip install -e ".[agent]"    # pydantic-ai agent stack (Assumption A2)
#   pip install -e ".[viz]"      # matplotlib figure rendering
